<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.SoftwareAccountingAndroid"
        tools:targetApi="31">

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AllRequestsActivity"
            android:theme="@style/Theme.SoftwareAccountingAndroid"
            android:exported="false" />

        <activity android:name=".RegistrationActivity" />
        <activity android:name=".UserMainActivity" />
        <activity android:name=".AdminMainActivity" />
        <activity android:name=".CreateRequestActivity" />

    </application>

</manifest>